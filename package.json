{
  "name": "tzo-powered-uqm-comm-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "ts-node build_vm_code.ts",
    "build-speech": "questmark --input asking-about-flowers/questmark_comm/spathi2.md --output ./speech.json --no-run",
    "go": "npm run build && npm run build-speech && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "questmark": "0.0.5",
    "raylib": "^0.6.0",
    "tzo": "^1.0.5",
    "uqm-files-parsers": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}